meta {
  name: Get Users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/users?includes=tickets
  body: none
  auth: bearer
}

params:query {
  includes: tickets
}

auth:bearer {
  token: {{bearerToken}}
}
